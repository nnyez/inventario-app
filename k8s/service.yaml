apiVersion: v1
kind: Service
metadata:
  name: inventario-app
  namespace: inventario-app
  labels:
    app: inventario-app
spec:
  selector:
    app: inventario-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
